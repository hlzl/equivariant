name: "tinyimagenet"
path: "./tiny-imagenet-200"
image_size: 64
train_subset: 0.9   # portion of the train set actually used for training
val_subset: 0.1   # portion of the train set used for validation